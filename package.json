{
  "name": "sky-white-trip-auditor",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "fix:firefox-json": "node scripts/fix-firefox-json.js",
    "build:icons": "node scripts/generate-icons.js",
    "build": "npm run fix:firefox-json && npm run build:icons && vite build && node scripts/postbuild.js",
    "build:ext": "npm run build",
    "postinstall": "npm run fix:firefox-json",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.3",
    "sharp": "^0.34.5",
    "svelte": "^5.0.0",
    "tailwindcss": "^3.4.17",
    "vite": "^6.0.0"
  }
}
